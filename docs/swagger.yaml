openapi: 3.1.0
info:
  version: 1.0.0
  title: Students App
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
  description: This is documentation of students app
tags:
  - name: Students
    description: Operations about users.
  - name: Auth
    description: Auth operations.
servers:
  - url: http://localhost:3000
  - url: https://contactsapp-go5k.onrender.com
paths:
  /students:
    get:
      $ref: ./paths/students/get.yaml
    post:
      $ref: ./paths/students/post.yaml
  /students/{id}:
    get:
      $ref: ./paths/students/{studentId}/get.yaml

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
