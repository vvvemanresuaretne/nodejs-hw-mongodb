# See https://redocly.com/docs/cli/configuration/ for more info
apis:
  student@v1:
    root: docs/openapi.yaml
extends:
  - recommended
rules:
  no-unused-components: error
openapi:
  theme:
    htmlTemplate: ./docs/index.html
    colors:
      primary:
        main: '#32329f'
    generateCodeSamples:
      languages:
        - lang: curl
        - lang: Node.js
        - lang: JavaScript
